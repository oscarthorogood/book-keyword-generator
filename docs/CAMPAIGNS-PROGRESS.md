# Campaigns Build Progress

Tracks `docs/CAMPAIGNS-SPEC.md` §8's build order. Update in the same PR as the work it reflects.

Rows through PR 10 shipped and merged on PR [#42](https://github.com/oscarthorogood/book-keyword-generator/pull/42). PR 3.5 (human upload gate) was confirmed by the user, unblocking PR 7/9/11. Work resumed on the same branch name, restarted from `main`, as PR [#43](https://github.com/oscarthorogood/book-keyword-generator/pull/43) — still one branch/PR per user decision rather than one per row.

- [x] **PR 0** — Preflight: confirm live-DB unknowns, write `docs/CAMPAIGNS-PREFLIGHT.md`. No code.
- [x] **PR 1** — `competitor_keywords` corrective migration (`sql/20-fix-competitor-keywords-drift.sql`). Also bundled the `sql/19` source-check drift found in the same audit, by user decision.
- [x] **PR 2** — Extracted the bulksheet column contract into `lib/bulksheetSchema.ts` (columns, row type, `PRODUCT`, `toCsv`, and one pure builder per entity: campaign/ad group/keyword/negative/product-targeting). `lib/bulksheet.ts` now calls these instead of building rows inline. No behaviour change — `tests/listingPipeline.test.ts`'s existing row/CSV assertions pass unchanged, plus 8 new tests in `tests/bulksheetSchema.test.ts`.
- [x] **PR 3** — Prerequisite A, split into 3a/3b for diff size. 3a: `lib/bulksheetSchema.ts` upload row builders (Product Ad entity/SKU, `Operation: "Create"`, camelCase negative match types, no `Source` column) — minimal fix for the 4 named bugs, not full template parity. 3b: `lib/bulksheetUpload.ts` (`buildUploadRows`, mirrors the review assembly logic without touching it) + `lib/bulksheetXlsx.ts` (`exceljs` renderer) + both export routes now accept `?format=upload`. Review CSV unchanged in content; filename suffix changed `-bulksheet.csv` → `-review.csv` to name it explicitly as the audit-trail half of the pair.
- [x] **PR 3.5** — Human gate: confirmed by the user.
- [x] **PR 4** — `NegativeKeyword.scope` (`campaign`/`ad_group`, defaults `ad_group`), new `Campaign Negative Keyword` entity/builders in `lib/bulksheetSchema.ts`, and both `addNegativeRows` (review + upload) route by scope. `lib/negativeKeywords.ts` untouched (stays campaign-unaware) per spec §1.3.
- [x] **PR 5** — Migrations `sql/21`-`26`, one concern per file, trigger drop first (fixed via `DROP FUNCTION ... CASCADE` rather than a `DROP TRIGGER ... ON campaigns` that would fail pre-table-creation). Schema reflects the §10 decisions above (currency columns have no `DEFAULT`, $25 budget, 30% ACOS). **Applied to Supabase** (`sql/20`-`27`, by explicit user request overriding the "human applies migrations" rule) — verified live: all 6 new tables, both rollup views, the 3 new `books` columns, both partial unique indexes on `keyword_recommendations`, old trigger/function confirmed gone. Post-apply security advisor caught an ERROR-level RLS bypass on the two rollup views (Postgres views default to the *creator's* permissions, not the querying user's, without `security_invoker = true`) — fixed immediately via `sql/27-fix-rollup-view-rls-bypass.sql`, also applied. Re-ran the advisor after: clean except pre-existing warnings unrelated to this work.
- [x] **PR 6** — `lib/campaignSelection.ts`: five pure selection functions + `toModifiedBroadSyntax`, reusing `scoreForRank()`. Both spec-named bug fixes applied (Brand Guard filter-before-slice, independent mega-bestseller/race-to-bottom rules). One spec/code contradiction fixed (code wins): `"comp-name"` is a `KeywordSource`, not `KeywordCategory`.
- [x] **PR 7** — Create Campaign end-to-end, split 7a-7d. 7a: `lib/campaignBulksheetPlan.ts` (`buildCampaignPlans`) assembles all five selectors' output into per-campaign targets/negatives, skipping empty campaigns, applying the Alpha Exact → BMM Discovery negative-exact safeguard automatically. 7b: `lib/campaignBulksheetExport.ts` renders a plan into both bulksheet shapes (review CSV + upload xlsx), reusing `lib/bulksheetSchema.ts`'s builders directly — a new assembly path, since the 5-campaign structure isn't the old 4-campaign one. 7c: `POST /api/books/[id]/campaigns` — loads the bank, builds `BookAnchors` (same mapping as the filter route), requires `confirmed: true` above the $50/day threshold, insert-as-draft-then-flip-to-exported, first real write to the `bulksheets` Storage bucket in this codebase. 7d: `CampaignActions.tsx` ("Create campaign" button + confirmation flow), wired into `KeywordManager.tsx`.
- [x] **PR 8** — Results import, split 8a/8b. 8a: `lib/searchTermImport.ts` now captures campaignName/adGroupName/targeting/matchType/impressions/sales (previously discarded); new `lib/resultsMatching.ts` (`aggregateByTarget` + `matchResultRows`), pure. 8b: `POST /api/books/[id]/results/import` (multipart, sha256 dedupe, batched upsert, last_* cache refresh) + `GET .../imports/[importId]` poll route. New `lib/csv.ts` (no CSV dep existed) and `lib/marketplaceCurrency.ts` (resolves decision 3, reusable by PR 7). **Architecture deviation from spec** (confirmed with user): processed synchronously within `maxDuration=60`, not a real background job — this codebase has no queue infra, every other long-running route uses this same pattern. Also corrected `sql/23`'s unique index (was `coalesce(...)`-based, unusable by Supabase-js's `.upsert`) since it wasn't yet applied.
- [x] **PR 9** — Update Campaign + diff, split 9a-9d. 9a: `lib/campaignDiff.ts` (`diffCampaignTargets`/`targetKey`), pure, diffs by id per spec. 9b: `lib/campaignBulksheetExport.ts` gained `buildUpdateReviewRows`/`buildUpdateUploadRows`, reusing the existing entity-row builders extended with an `operation` param (`lib/bulksheetSchema.ts`) rather than forking a second builder set, per spec §4 step 3. Extracted shared bank/asinBank/siblingBooks/negatives/anchors loading (previously inline in the POST route) into `lib/campaignContext.ts` (`loadCampaignContext`) so Create and Update read identical live state. 9c: `PATCH /api/books/[id]/campaigns/[campaignId]` — reconstructs the last-exported snapshot from `campaign_targets` (latest row per target key, already-archived rows dropped so they aren't re-emitted forever), re-runs the single selector for that campaign's type, diffs, and uploads only the changed rows. **Deviates from the spec's literal step 4** ("insert a new campaigns row"): `campaigns_unique_name_per_user` (sql/21, already live) makes a second row with the same name impossible, and the name must stay stable for Search Term Report matching (spec §5 step 5) — updates the existing row in place instead (new `export_batch_id`, `operation: 'update'`, refreshed bulksheet), which also keeps `amazon_campaign_id` without re-pasting it every time. Only the five single-ad-group campaign types are supported (Auto Discovery blocked with a clear message — its multiple ad groups don't fit the single-`adGroup`-param Update builders). Added `SINGLE_AD_GROUP_LABEL` to `lib/campaignSelection.ts` so Create and Update share one ad-group-name-per-type mapping. 9d: `GET /api/books/[id]/campaigns` (list, for the action row + badge) and `CampaignActions.tsx` gained the Amazon Campaign ID input/save and the "Update campaign" button (disabled until the ID is set and status is `exported`).
- [x] **PR 10** — Recommendation engine, split 10a/10b-1/10b-2/10c. 10a: `lib/recommendations.ts` (`recommendForKeyword`/`recommendForCompetitorAsin`, all 4 spec-named fixes). 10b-1: `GET /recommendations` generates+dedupes+persists (also fixed a gap in `sql/26` — the partial unique index only covered keywords, not competitor ASINs). 10b-2: accept (per-type apply, archive-and-block inserts a `negative_keywords` row, `promote_to_alpha_exact` requires `confirm:true` and only completes the keyword-level half pending PR 7's `campaign_targets`)/reject/export routes. 10c: `RecommendationsPanel.tsx`, wired into `KeywordManager.tsx`.
- [x] **PR 11** — `/campaigns`, `/campaigns/[id]`, sidebar. `GET /api/campaigns` (cross-book list, joined with book title/author) and `GET /api/campaigns/[id]` (one campaign + book + `campaign_targets` export history + a lifetime `campaign_results` rollup computed server-side). `CampaignsOverviewPage.tsx` groups by `export_batch_id`, mirrors `CompetitorsOverviewPage.tsx`'s read-only aggregate-table shape. `CampaignDetailPage.tsx` shows the overview, lifetime results, scoped recommendations, and export history. `RecommendationsPanel.tsx` gained an optional `campaignId` prop that filters its existing book-scoped fetch client-side rather than adding a second endpoint. Sidebar entry added to `AppShell.tsx` per spec §7.

All 11 build-order rows complete. `npm run validate` (type-check, lint, `next build`, vitest) green — 486 tests across 38 files, no changes to pure logic in this PR's work.

## Open decisions (spec §10) — resolved

1. **Auto Discovery**: keep, optional and off by default (spec's own recommendation).
2. **Budget**: $25/campaign default (≈$125/day total across 5), typed confirmation required above $50/day.
3. **Currency/marketplace**: per-book, derived from `books.marketplace` at write time — no hardcoded schema default (`campaigns.currency`/`campaign_results.currency` are `NOT NULL` with no `DEFAULT`, forcing every insert to supply it explicitly).
4. **`series_key`**: manual per book; UI suggests author name as a default, human confirms/edits (spec §2.5's own recommendation).
5. **Target ACOS**: flat 30% default (`books.target_acos NUMERIC DEFAULT 0.30`).
6. **Rival exclusion thresholds**: `maxBsr: 500`, `minPrice: 2.99` (spec's own defaults).
7. **Recommendation cooldown**: 30 days after rejection.
